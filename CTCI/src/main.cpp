//
// Created by sajith on 11/29/21.
//

//# include "copy_trans.h"
#include <bits/stdc++.h>

using namespace std;

#define M1(x) (x ==0)? 1: x* dosm(x-1);
#define M2(x) (static_cast<bool>(x));
#define M3(x) (si+x);

static int si;

int dosm(int x)
{
    return M1(x);
}

int main()
{
    cout<<sizeof(string);

    return 0;
}